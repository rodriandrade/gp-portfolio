import styled from 'styled-components'

const TitleContact = styled.h2`
    font-size: 18px;
    color: rgb(50,50,50);
    margin:0;
`

const IconContact = styled.img`
    width:20px;
`

const SubtitleContact = styled.h2`
    font-size: 16px;
    color: rgb(50,50,50);
    margin:0;
`

const ContainerContact = styled.div`
    display: flex;
    flex-direction: column;
`

export { TitleContact, IconContact, SubtitleContact, ContainerContact }